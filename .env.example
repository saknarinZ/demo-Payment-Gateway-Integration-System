# =============================================================================
# Payment Gateway Integration System - Environment Variables
# =============================================================================
# คัดลอกไฟล์นี้เป็น .env และใส่ค่าจริง
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=payment_gateway
MYSQL_USER=payment_user
MYSQL_PASSWORD=payment_secret

# -----------------------------------------------------------------------------
# Omise (Opn Payments) API Keys
# ดู Keys ได้ที่: https://dashboard.omise.co/settings/keys
# -----------------------------------------------------------------------------

# Public Key - ใช้ใน Frontend สำหรับ Tokenization
# รูปแบบ: pkey_test_xxxxx (Test) หรือ pkey_xxxxx (Live)
OMISE_PUBLIC_KEY=pkey_test_your_public_key_here

# Secret Key - ใช้ใน Backend สำหรับ Charge
# รูปแบบ: skey_test_xxxxx (Test) หรือ skey_xxxxx (Live)
OMISE_SECRET_KEY=skey_test_your_secret_key_here

# -----------------------------------------------------------------------------
# Webhook Configuration
# -----------------------------------------------------------------------------
# Secret สำหรับ Verify Webhook Signature
OMISE_WEBHOOK_SECRET=your_webhook_secret_here

# Webhook Endpoint (สำหรับ Omise Dashboard)
# Development: https://your-ngrok-url.ngrok.io/api/v1/webhooks/omise
# Production: https://your-domain.com/api/v1/webhooks/omise

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
SPRING_PROFILES_ACTIVE=development
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# Redis (Optional - สำหรับ Caching)
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
